import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    deliveredMsgsCount : {}
}

const unreadMsgsSlice = createSlice({
    name:'unreadCount',
    initialState,
    reducers:{
        
    }
})
